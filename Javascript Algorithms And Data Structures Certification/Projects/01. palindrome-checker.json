{"index.js":"function palindrome(str) {\n  // Good luck!\n  const originalStr = str.replace(/[^0-9a-zA-Z]/g, '').toLowerCase();\n  const reversedStr = originalStr.split('').reverse().join('');\n  return originalStr === reversedStr;\n}\n\n\n\npalindrome(\"eye\");"}